# ============================================
# DocuMind - Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit your .env file to version control!

# ============================================
# Server Configuration
# ============================================
PORT=5001
NODE_ENV=development

# ============================================
# Authentication & Security
# ============================================
# Generate a strong random secret (32+ characters recommended)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# MongoDB Configuration
# ============================================
# Get your MongoDB connection string from:
# https://www.mongodb.com/cloud/atlas
# 
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=documind

# ============================================
# Pinecone Configuration
# ============================================
# Get your API key from: https://app.pinecone.io/
# 
# The app will automatically create an index named 'ai-doc-chat' with:
# - Dimensions: 1024
# - Metric: cosine
# - Cloud: aws (region: us-east-1)
PINECONE_API_KEY=pcsk_your_pinecone_api_key_here

# Optional: Specify custom index name (default: ai-doc-chat)
# PINECONE_INDEX=ai-doc-chat

# ============================================
# Google Gemini AI Configuration
# ============================================
# Get your API key from: https://makersuite.google.com/app/apikey
# 
# You can use either GEMINI_API_KEY or GOOGLE_AI_API_KEY
# The app will check both (GEMINI_API_KEY takes precedence)
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative key name (if you prefer)
# GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# ============================================
# File Upload Configuration (Optional)
# ============================================
# Maximum file size in bytes (default: 10MB = 10485760 bytes)
MAX_FILE_SIZE=10485760

# Directory for temporary file uploads (default: uploads)
UPLOAD_DIR=uploads

# ============================================
# Additional Notes
# ============================================
# 
# 1. MongoDB Atlas Setup:
#    - Create a free cluster at https://www.mongodb.com/cloud/atlas
#    - Add your IP address to Network Access
#    - Create a database user with read/write permissions
#    - Get the connection string and replace <username> and <password>
#
# 2. Pinecone Setup:
#    - Sign up at https://www.pinecone.io/
#    - Create a new project (free tier available)
#    - Copy your API key from the dashboard
#    - The app will auto-create the index on first use
#
# 3. Google Gemini Setup:
#    - Visit https://makersuite.google.com/app/apikey
#    - Create a new API key
#    - Note: Free tier has rate limits and quotas
#
# 4. Security Best Practices:
#    - Use strong, unique passwords for all services
#    - Rotate JWT_SECRET regularly in production
#    - Enable HTTPS/TLS in production
#    - Never commit .env files to git (already in .gitignore)
#    - Use environment-specific secrets in production (AWS Secrets Manager, etc.)
